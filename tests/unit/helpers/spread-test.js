
import { spread } from 'dummy/helpers/spread'
import { module, test } from 'qunit'

module('Unit | Helper | spread')

// Replace this with your real tests.
test('it works', function (assert) {
  let result = spread([42])
  assert.ok(result)
})

